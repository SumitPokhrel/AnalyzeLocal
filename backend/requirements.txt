# Web server and API
fastapi>=0.115
uvicorn>=0.34
python-multipart>=0.0.20

# HTTP client used to reach the local Ollama server on loopback
httpx>=0.28

# Document text extraction
pypdf>=5.1
python-docx>=1.1

# Local NER model for unstructured PII. Pulls in torch, which is a large
# download on first install.
gliner>=0.2.13
